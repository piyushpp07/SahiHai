# Frontend Environment Configuration

# Local Development - Use local backend IP (Update with your machine's IP)
# Run: ifconfig | grep "inet " | grep -v 127.0.0.1
EXPO_PUBLIC_API_URL=http://192.168.1.39:5051

# Production - Use deployed backend URL (when ready)
# EXPO_PUBLIC_API_URL=https://sahi-hai-tau.vercel.app
