import React from 'react';
import { View, Text } from 'react-native';

export default function ScamTab() { const { colors } = useTheme();
  return (
    <View>
      <Text>Scam Tab</Text>
    </View>
  );
}